<form method="post" action="{{ path('app_nutrition_delete', {'id': demande_regime.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_regime.id) }}">
    <button class="btn">Delete</button>
</form>

